import { EntryTimePipe } from './entry-time.pipe';

describe('EntryTimePipe', () => {
  it('create an instance', () => {
    const pipe = new EntryTimePipe();
    expect(pipe).toBeTruthy();
  });
});
